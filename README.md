# Stable Fluids (Web)

Jos Stam の論文 *Stable Fluids (2001)* の手法（拡散・移流・圧力投影）をベースに、ブラウザ上で動く 2D 流体シミュレーションを実装したサンプルです。

## 使い方

`index.html` をブラウザで開くだけで動作します。

- ドラッグ: 流体へ力と染料を注入
- `R` キー: 状態をリセット
- ウィンドウリサイズ: 自動でキャンバス／グリッドを再生成

## ローカルサーバーで実行（任意）

```bash
python3 -m http.server 4173
```

その後 `http://localhost:4173` を開いてください。
